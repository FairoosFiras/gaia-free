# Cloud Run base configuration (shared across all deployed environments)
# Environment-specific files (stg.env, prod.env) override these values.
# Copy to cloudrun.base.env and update with your project values.
# Or run `make setup-private` if you have access to gaia-private.

APP_MODULE=gaia.api.app:socket_app
WORKERS=1

# Storage configuration (Cloud Run read-only FS; use GCS)
CAMPAIGN_STORAGE_PATH=/tmp/campaigns
CAMPAIGN_STORAGE_BOOTSTRAP=lazy
CAMPAIGN_STORAGE_BACKEND=auto

# Auth0 configuration (get these from your Auth0 dashboard)
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://your-api-identifier
AUTH_AUTO_PROVISION_USERS=true

# Email configuration (non-sensitive)
EMAIL_PROVIDER=smtp
SMTP_PORT=2525
EMAIL_FROM_ADDRESS=admin@your-domain.com
EMAIL_FROM_NAME=Your App Admin

# Cloud SQL (connection name format: project:region:instance)
DB_INSTANCE_CONNECTION_NAME=your-project:your-region:your-instance
POSTGRES_USER=your_db_user
POSTGRES_DB=your_db_name

# GCP Project
GCP_PROJECT_ID=your-gcp-project-id
